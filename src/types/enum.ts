export enum OrderingType {
    Food = 'food',
    Drink = 'drink',
}

export enum SocketActionType {
    Create = 'Create',
    Update = 'Update',
    Remove = 'Remove',
}

export enum MonthlyOrderStatus {
    Pending = 'pending',
    Ordering = 'ordering',
    Completed = 'completed',
}
